<div style="padding: 80px;">

	<h1>Analysis</h1>

	<h4 style="color: grey;">Based on your selection of images, we think you like:</h4>

	<h3 class="titles2" style="color: white;"><%= @category.name %> Tattoos</h3>

	<p style="color: #AAAAAA;"> <%= @category.description %> </p>

	</h3> More <%= @category.name %> Tattoos <h3>

	<% @samples.all.each do |image| %>
	<%= image_tag(image.path + image.name, {class: "medium", id: "id"}) %>
	<% end %>


	</h3> Artists we think you'll like: <h3>

	<% @artists.all.each do |artist| %>

	<div class="artist-suggestion" id="<%= artist.id %>">
		
		<h2><%= artist.name %></h2>
		<h2><%= artist.address1 %></h2>
		<h2><%= artist.email %></h2>

		2 images by this artist:
		<% @images = [] %>
		<% @images.push(Image.find(artist.id*6)) %>
		<% @images.push(Image.find(artist.id*6+1))%>

		<% @images.each do |image| %>
		<%= image_tag(image.path + image.name, {class: "medium", id: "id"}) %>
		<% end %>

		Add like button!
	</div>

	<% end %>

</div>